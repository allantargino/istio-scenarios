---
apiVersion: config.istio.io/v1alpha2
kind: RouteRule
metadata:
  name: frontend-default
spec:
  destination:
    name: frontend
  precedence: 1
  match:
    request:
      headers:
        uri:
          prefix: /frontend/
  rewrite:
    uri: /
  route:
  - labels:
      version: v1
---